# Secrets — never commit real keys (§29)
ai-api/.dev.vars
*.env
.env.*

# Dependencies / build output
node_modules/
dist/

# Wrangler local state (cache/tmp — never needed in the repo)
ai-api/.wrangler/

# OS + editor noise
.DS_Store
Thumbs.db
*.log
